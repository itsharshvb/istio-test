# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/
# Helm dependency charts - we keep these for now
# charts/*.tgz
# Documentation
*.md
LICENSE
# Package files (but not in charts directory)
/*.tgz
# Test files
*_test.go
# CI/CD files
.github/
.gitlab-ci.yml
# Other files
OWNERS
Makefile
